import DIcon from "../DIcon";

const TableIcon = () => (
  <DIcon icon="fa-table" cdi={false} classCustom="!-mx-0 !text-lg"></DIcon>
);

const CardIcon = () => (
  <DIcon icon="fa-grid-2" cdi={false} classCustom="!-mx-0 !text-lg"></DIcon>
);

const ListIcon = () => (
  <DIcon icon="fa-list" cdi={false} classCustom="!-mx-0 !text-lg"></DIcon>
);

export { CardIcon, ListIcon, TableIcon };
